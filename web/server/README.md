Luồng xử lý....

người dùng gửi input => tiền xử lý(Middleware) => gửi cho bộ xử lý trung tâm(ApplicationServicer) => gọi và sử dụng các thuật toán. (algorithyms) => gửi cho bộ trả về và viết thành api cho người dùng,

// đã xong các cơ bản.
// Thiếu deploy + test + docx
// thiếu một số thuật toán khác thôi